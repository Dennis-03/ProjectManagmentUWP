<Page
    x:Class="ProjectManagmentApp.View.SignIn"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectManagmentApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource SecondaryColor}">

    <Grid>
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Height="320"
                    Width="500"
                    Padding="20"
                    Background="{StaticResource PrimaryColor}"
                    CornerRadius="5">
            <TextBlock x:Uid="Login"
                       Style="{StaticResource TitleTextBlockStyle}"
                       TextAlignment="Center"/>
            <StackPanel Height="30">
                <TextBlock Name="ResponseTextBlock"
                           Foreground="Red"
                           TextAlignment="Center"/>
            </StackPanel>
            <TextBox Name="Username"
                     x:Uid="Username"
                     Margin="20"/>
            <PasswordBox Name="Password"
                         x:Uid="Password"
                         Margin="20,0"/>
            <Button Name="Submit"
                    x:Uid="Submit"
                    Style="{StaticResource MyButtonStyle}"
                    Click="Submit_Click"
                    HorizontalAlignment="Center"
                    Background="{StaticResource PrimaryColor}"
                    Foreground="{StaticResource TertairyColor}"
                    BorderBrush="{StaticResource TertairyColor}"
                    CornerRadius="3"
                    Margin="20"/>
            <Button Name="SignUp"
                    x:Uid="SignUp"
                    Style="{StaticResource MyButtonStyle}"
                    Click="SignUp_Click"
                    Background="{StaticResource PrimaryColor}"
                    HorizontalAlignment="Center"
                    Foreground="{StaticResource TertairyColor}"/>
        </StackPanel>
    </Grid>
</Page>
